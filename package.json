{
  "name": "ai-realestate-investment",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered real estate investment platform",
  "workspaces": [
    "src/backend",
    "src/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=src/backend & npm run dev --workspace=src/frontend",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=src/backend"
  }
}
